	* Add to the parser the configuration for device to grab the ip and select from
	* start to develop the database part and interpretation
	* add language to the configuration parser
	* add recording limit to 30 s
